import { redirect } from 'next/navigation';

// `/manager/me` → `/profile` (задача 3045, §1). Адрес кабинета переехал; здесь
// остаётся редирект, потому что на него ведут закладки, шаблон дайджеста бота и
// PWA-иконки, поставленные на телефон до переезда. Логика «чью карточку показать»
// живёт в `lib/org/selfCard.ts` и вызывается из `/profile` и `/bx/manager`.
//
// `?tab=` и прочие параметры НЕ протаскиваем: серверный редирект их и так сохранит
// только если написать это руками, а единственная внешняя ссылка с параметрами
// (дайджест бота, `?tab=customers&customer=...`) переведена на `/profile` в самом
// шаблоне — см. lib/jobs/managerDigest.ts.
export default function Page() {
  redirect('/profile');
}
