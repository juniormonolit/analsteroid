import { RepeatReportPage } from '@/features/reports/ui/RepeatReportPage';

// Раздел «Продажи → Повторные» (#1725). Флагманские отчёты по повторным продажам:
// Repeat Rate, комплексность, время до/между заказами, триптих касаний.
export default function Page() {
  return <RepeatReportPage />;
}
