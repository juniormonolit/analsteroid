-- 179: metrics.human_description — «Как считается» человеческим языком (задача
--      владельца 11.08: «каждая метрика при нажатии на вопросик — как она считается
--      человеческим языком»). БД: analytics.
--
-- Отдельная колонка, а НЕ переписывание description: description — техническая
-- памятка (ссылки на задачи, номера миграций, поля БД), она нужна разработчикам и
-- админам как была. human_description — текст для МЕНЕДЖЕРА, показывается по «?» у
-- метрики в панели выбора метрик отчёта.
--
-- Заполнение — скриптом scripts/fill-human-descriptions.mjs (генерирует текст из
-- определения метрики: источник, агрегат, поле даты, фильтры, формула). Колонка
-- редактируется руками в «Настройки → Метрики» — скрипт НЕ перезаписывает непустые
-- значения (ручная правка владельца всегда побеждает генератор).
--
-- DOWN: ALTER TABLE metrics DROP COLUMN IF EXISTS human_description;

ALTER TABLE metrics ADD COLUMN IF NOT EXISTS human_description text;

COMMENT ON COLUMN metrics.human_description IS
  'Как считается метрика — человеческим языком, для «?» в панели метрик. Пусто → UI падает на description. Заполняется scripts/fill-human-descriptions.mjs, ручные правки скрипт не трогает.';
