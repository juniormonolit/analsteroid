-- «Границы» (правка владельца 09.07, встреча вечер, п.4): режим вертикальных/горизонтальных
-- границ таблицы отчёта — опция в объединённой панели «Настройки отчёта» → «Вид», по
-- образцу zebra (migration 057). Значения: 'grid' (дефолт — полная сетка, горизонтальные +
-- вертикальные между колонками метрик) / 'horizontal' (только горизонтальные — было
-- единственным поведением до этой правки) / 'none' (без границ).
-- NULL = 'grid' (см. SalesReportPage: setBorderMode(preset.borderMode ?? 'grid')) — старые
-- сохранённые отчёты без этого поля получают НОВЫЙ дефолт (полная сетка), а не старое
-- поведение: до этой правки вертикальных границ между метриками не было вовсе, поэтому
-- «читать как раньше» не имеет смысла — раньше такой настройки просто не существовало.
-- БД: YC system (run_system.mjs).
ALTER TABLE saved_reports ADD COLUMN IF NOT EXISTS border_mode text;
