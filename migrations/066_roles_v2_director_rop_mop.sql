-- Права v2 (owners-inbox/analsteroid-edits-spec-20260709.md, раздел «Права v2»).
-- Добавляет 3 новые системные роли: Директор, РОП, МОП.
--
-- ВАЖНО: существующие роли (Администратор, Пользователь) этой миграцией НЕ
-- трогаем — ни permissions, ни любые другие поля. Только INSERT новых строк
-- (ON CONFLICT (name) DO NOTHING — идемпотентно, повторный запуск безопасен).
-- Ни один существующий пользователь роль не меняет — переназначение вручную,
-- владельцем (users.role_id этой миграцией не апдейтится).
--
-- Дефолтные наборы прав — литеральные (роли не наследуют друг друга, permissions
-- у каждой роли — самостоятельный text[]), по спеке «дельт» от базовой видимости
-- «Продажи»:
--   МОП       = Продажи (то же, что и «Пользователь» по спеке; своя карточка в ЛК —
--               отдельная фича карточки менеджера, не про раздел)
--   РОП       = Продажи + Планы
--   Директор  = Продажи + Планы + Декомпозиция
-- Права на РЕДАКТИРОВАНИЕ (action.plans.edit) новым ролям по умолчанию НЕ
-- выдаются — только просмотр Планов; если нужно иначе, супер-админ донастроит
-- через матрицу прав (/settings/rights-matrix).
--
-- Текущая роль «Пользователь» на проде шире, чем «Продажи+ЛК» из спеки
-- (уже включает marketing/summary/plans/decomposition/action.plans.edit —
-- см. WORKLOG/отчёт задачи). Это НЕ регресс и НЕ ошибка миграции: спека
-- описывает дефолты для НОВЫХ ролей, а не переопределяет то, что уже роздано.
-- Если владелец решит сузить «Пользователь» или «Администратор» до спеки —
-- это делается вручную через матрицу прав (человеческое решение, не авто).
--
-- is_system = true — как и у Администратор/Пользователь: защищено от удаления
-- через UI, пока роль назначена хотя бы одному пользователю (и т.к. системная
-- роль вообще не даёт кнопку «Удалить» — см. app/api/admin/roles/[id]/route.ts).
--
-- БД: YC system. НЕ применять локально — накатывает Артём на проде атомарно.

INSERT INTO roles (name, description, is_system, permissions) VALUES
('МОП', 'Менеджер отдела продаж — видит Продажи, свою карточку в ЛК', true,
 ARRAY['section.sales']),
('РОП', 'Руководитель отдела продаж — Продажи + Планы своего отдела', true,
 ARRAY['section.sales', 'section.plans']),
('Директор', 'Директор направления — Продажи + Планы + Декомпозиция', true,
 ARRAY['section.sales', 'section.plans', 'section.decomposition'])
ON CONFLICT (name) DO NOTHING;
