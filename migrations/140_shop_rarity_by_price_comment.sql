-- Правка владельца 04.08 к задаче 2960 (задача 2983): редкость товаров/бустов
-- магазина теперь считается ОТ ЦЕНЫ (features/shop/engine/rarity.ts,
-- rarityForPrice), а НЕ от min_level. min_level остался ЧИСТО антифарм-
-- защитой («порог доступности» — чтобы менеджер хитростью за два месяца не
-- нафармил айфон), покупка ниже порога блокируется на бэкенде (app/api/shop
-- POST), карточка на витрине ниже порога блюрится с подписью «Доступен с N
-- уровня». Никаких изменений данных/структуры — только исправление
-- COMMENT ON COLUMN, оставленного миграцией 139 (тогда редкость ЕЩЁ считалась
-- от min_level, комментарий вводил в заблуждение).
-- СИСТЕМНАЯ БД (YC) — применяется вручную migrations/run_system.mjs,
-- ОТДЕЛЬНО на dev (junibaseone) и на prod (system).
-- DOWN: вернуть текст комментария из 139 (см. git history этого файла) — чисто
-- документационно, данные/структуру эта миграция не трогает.

COMMENT ON COLUMN shop_items.min_level IS
  'Мин. уровень XP (features/xp/engine/xp.ts levelFromXp) для покупки — ЧИСТО антифарм-защита ("порог доступности"), с 2983 НЕ влияет на редкость. Редкость считается от цены (price_units/priceEball), features/shop/engine/rarity.ts rarityForPrice(). Ниже порога карточка на витрине блюрится ("Доступен с N уровня"), покупка блокируется и на бэкенде (app/api/shop POST).';
