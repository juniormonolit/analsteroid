# Бэклог проекта

Единственный актуальный список отложенного и недоделанного. Заведён 06.08.2026:
до этого бэклог жил в памяти Claude (`~/.claude/.../memory/backlog-*.md` — локально
на машине владельца, вне git, недоступно другим сессиям и агентам) и кусками в
`WORKLOG.md` среди 15 тысяч строк. Теперь — здесь, в репозитории.

**Правила ведения.** Новая отложенная задача → строка сюда, а не только в память.
Задача взята в работу → отмечать прямо в пункте; сделана → удалять отсюда, подробности
идут в `WORKLOG.md`. Формулировки владельца сохранять дословно там, где они задают
рамку — пересказ теряет смысл, а решение потом принимается по пересказу.

---

## 1. В работе прямо сейчас

### Ничего не в работе
Конструктор «Мой отчёт» закрыт целиком 06.08.2026 (все 6 шагов спеки), подробности
в WORKLOG. **Осталось действие вне кода:** накатить миграцию 156
(`report_templates`) на dev и прод — до неё личные шаблоны не сохраняются,
пресеты по роли работают. Раздел живьём под сессией ещё не открывали.

---

## 2. Запланировано, ждёт очереди

### Персонализация профиля (задача #34)
Рамки аватара и эмодзи-фоны, покупаемые за MLT; раздел в настройках ЛК. Часть
списка владельца от 05.08 по ЛК-соцсетке, единственный невыполненный пункт из него.

### Уведомления (задача #36)
Бейджи непрочитанного у пунктов рельсы, подсветка нового, read-state. Висящий
колокольчик из профиля убран по требованию владельца. Основное строила
параллельная сессия — **сначала проверить, что уже есть, потом доделывать**.

### ЛК не-продажников (задача #41)
Владелец: «где нет данных — так и написать: нет данных». Компонент
`components/ui/NoData.tsx` есть, применён в двух местах (полоса план/факт,
«Мои заказчики»). Осталось: **статистика и радар навыков**. Масштаб — 100+
активных сотрудников вне продаж. ЛК им не отрезаем: смотрят «Движуху», ходят в
чужие профили, копят награды.

### Частота наград у медианного менеджера
Цель владельца — ништяк раз в 2–3 дня, сейчас раз в 4. Лечится не ценой, а
**количеством поводов**: новые награды и ежедневные квесты (владелец их и так
планирует). Держать в голове при добавлении любой механики.

### Инвайты и бот — мелкие хвосты
- показ ссылки-инвайта на экране для админа (для себя уже есть — `DirectAccessCard`;
  при тишине бота чужие инвайты по-прежнему теряются молча);
- чистка `bot_outbound_log`;
- автовыдача секретных ачивок за собранные сеты (`badge_definitions.set_of` есть,
  движка выдачи нет);
- кнопки подтверждения заявок прямо в боте.

---

## 3. Отложено владельцем — ФОРМАТ ОБСУДИТЬ ДО ПОСТРОЙКИ

Общее для всего раздела: владелец явно просил не начинать по своему усмотрению.
Строить только после разговора.

### Раздел «Обо мне»
Страничка о себе: ссылки и «ещё что-то». Владелец сам сомневается в нужности:
«хз надо ли. если не делать, то обсудить потом».

### Раздел «Обучение и грейды»
Формат не задан. Вероятная связь с существующими шкалами (уровни/титулы XP у
менеджеров, шкала тяги у руководителей Дрезина → Атомный ледокол) — но
подтверждения владельца на эту связь нет.

### Полный разбор функционала бота «Аналитик»
Владелец: «давай в бэклог и отдельно обсудим». Карта уже собрана —
`BOT_ANALITIK_MAP.md`, 14 дыр. Нужен разговор о том, что менять.
**Важно не забыть при разговоре: бот РАБОТАЕТ** — ежедневные отчёты владельцу и
Коваленко доходят (канал `report` не глушится); режим тишины гасит остальное.

### Конструктор уведомлений в ЛК
Бот шлёт сообщение при аномалии показателя, резком снижении/росте, отставании от
плана. Место в UI готово. Когда дойдёт очередь: миграция под `notification_rules`,
отправка через `sendBitrixBotMessage` (`lib/bitrix/notify.ts`), расчёты поверх
`lib/profile/deptSummary.ts`.

### Раздел «Воронка»
Формат не выбран: поток за период с конверсиями / текущий снимок пайплайна по
стадиям / обе вкладки. Данные готовы: `sa.stages`, `funnel_id`, стадийные даты в
deals (created/reserved/confirmed/sold/delivered/lost_at).

### Раздел «По заказчикам»
Формат не выбран: рейтинг клиентов / отчёт-конструктор / новые-vs-повторные.
Данные готовы: `bitrix_contacts` / `bitrix_companies` (бэкфилл есть),
`deal.contact_id` / `company_id`.

### Heatmap активности пользователя
ЛК с квадратиками по дням в стиле GitHub. Владелец допускал метрику попроще —
называл длительность активной сессии. Перед постройкой уточнить: (1) хватит ли
`user_sessions.created_at` + TTL куки как прокси длительности, или нужны
heartbeat-пинги; (2) где живёт — в настройках или на видном месте в профиле.

---

## 4. Известные ограничения, не требующие действий прямо сейчас

- **Цены наград применяются в момент начисления**, задним числом ничего не
  пересчитывается. Любой будущий ребаланс трогает только новые начисления.
- **TTL баллов = 12 мес** — регулятором расходов не является: симуляция года
  показала сгорание ≈ 0 при любом TTL, если люди тратят. Бьёт только по копящим.
- **Себестоимость товаров** (`shop_items.cost_rub`) заполнена не у всех позиций;
  где пусто, дашборд экономики оценивает по цене продажи × курс — завышает,
  о чём в UI есть оговорка.
